<% content_for :title, "Editing user session" %>

<h1>Editing user session</h1>

<%= render "form", user_session: @user_session %>

<br>

<div>
  <%= link_to "Show this user session", { action: :show } %> |
  <%= link_to "Back to user sessions", { action: :index } %>
</div>
