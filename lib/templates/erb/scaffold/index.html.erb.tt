<%% content_for :title, "<%= human_name.pluralize %>" %>

<h1><%= human_name.pluralize %></h1>
<%% columns = <%= attributes.reject(&:password_digest?).map(&:name).inspect %> %>
<%%= render "shared/paginated_table", records: @<%= plural_table_name %>, pagy: @pagy, columns: %>

<%%= link_to "New <%= human_name.downcase %>", <%= new_helper(type: :path) %> %>
