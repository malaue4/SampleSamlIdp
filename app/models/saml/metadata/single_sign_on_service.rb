module Saml
  module Metadata
    # The SingleSignOnService (SSO) endpoint is the entry point on the Identity Provider (IdP)
    # for handling authentication requests (AuthnRequests) initiated by Service Providers (SP).
    class SingleSignOnService < EndpointWithoutResponseLocation
    end
  end
end
