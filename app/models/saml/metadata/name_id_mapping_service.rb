module Saml
  module Metadata
    # The NameIDMappingService (NIMS) endpoint is used to map a persistent identifier
    # (NameID) from one name identifier format to another, which is useful for cross-domain identity mapping.
    class NameIdMappingService < EndpointWithoutResponseLocation
    end
  end
end
